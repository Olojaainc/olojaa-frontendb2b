export enum OrderStatus{
    pending = 'pending',
    completed = 'completed',
    cancelled = 'cancelled',
    recurring = 'recurring'
}

export enum GasTypes{
    LPG = 1,
    CNG = 2
}

export enum DeliveryEnum{
    recurring  = 'recurring',
}